package ch02.ex07;

/**
 * Streamのコンパニオンクラス
 *
 * @author 山田晃一
 */
public class Streams {
    /*
     * 実装不可能？有限ストリームにcountを使うなどして結果が得られればtrueだが、対象のstreamは終端操作により使えなくなる。 無限ストリームだとcountが返ってこない。
     */
    // public static <T> boolean isFinite(final Stream<T> stream) {
    //
    // }
}
